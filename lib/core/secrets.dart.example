// Copy this file to secrets.dart and fill in your values.
// `sentryDsn` may be left empty (`''`) — Sentry.init becomes a safe no-op then,
// which is the right default for local dev (no DSN = nothing is sent).
const supabaseUrl = 'https://YOUR_PROJECT.supabase.co';
const supabaseAnonKey = 'YOUR_ANON_KEY';
const sentryDsn = '';
